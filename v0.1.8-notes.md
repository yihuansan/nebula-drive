# NebulaDrive v0.1.8

本次更新聚焦**收藏页体验优化**与**账号管理修复**。

## ⭐ 收藏页操作栏重做

彻底重做收藏页的操作列，从混乱的按钮改为清晰的分层结构：

- **一级按钮**（直接可见）：
  - 文件夹：`重命名` `打开` `下载` `删除`
  - 文件：`预览` `下载` `删除`
- **二级菜单**（⋯ 按钮）：
  - 文件夹：移动 / 分享 / 取消收藏
  - 文件：重命名 / 移动 / 分享 / 取消收藏
- **布局优化**：按钮等宽对齐（72px），左右均匀分布，不再错位

## 🔧 导航栏用户名显示

侧边栏底部第二行从显示角色（如"管理员"）改为显示 **@用户名**，跟随用户实际名称变化。

## 🐛 修复：修改账号用户名不生效

**问题**：在"我的资料"→"修改账号"中修改用户名后，用户名不变（仍为 admin），但密码修改成功。

**原因**：后端 `updateUser` 函数的 SQL 语句缺少 `username` 列，导致用户名更新被忽略。

**修复**：`updateUser` 函数增加 `username` 字段支持，SQL UPDATE 语句加入 `username = ?`。

## 🐛 修复：登录历史显示所有账号

**问题**："我的资料"→"登录历史"显示所有账号的登录记录（包括错误用户名），而非仅当前账号。

**修复**：
- 后端 `listLoginLogs` 增加用户名过滤参数
- 路由增加 `mine` 查询参数：`mine=true` 时仅返回当前用户的登录记录
- 前端 Profile 页传 `mine=true`，只查自己的登录历史

管理员日志页不受影响，仍显示所有记录。

## 技术细节

- `user.service.ts`：`updateUser` 增加 `username` 字段
- `log.service.ts`：`listLoginLogs` 增加可选用户名过滤
- `log.routes.ts`：增加 `mine` 查询参数
- `Profile.vue`：登录历史请求加 `mine=true`
- `App.vue`：侧边栏底部第二行改为 `@username`
- `Favorites.vue`：操作列重做（固定宽度按钮 + ⋯ 二级菜单）
