## v0.1.8 更新内容

### 新增
- 在线更新逻辑修复：改用 Node.js 重启脚本（避免 PowerShell 编码问题）
- 先停止服务器再替换文件，避免 Windows 文件锁定
- 每步操作写入 update_debug.log，便于排查问题

### 修复
- 修复用户名修改不生效（updateUser SQL 缺少 username 列）
- 修复登录历史显示所有账号记录（现在只显示当前账号）
- 修复 package.json BOM 导致版本读取失败
- 修复 root package.json 路径错误

### 其他
- 删除仓库中的升级包 zip 文件（只保留在 GitHub Releases）
