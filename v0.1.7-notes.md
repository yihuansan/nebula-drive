# NebulaDrive v0.1.7

本次更新带来两项重要改动：**SMTP 邮件服务**（全新）与**系统设置全新布局**（重做）。

## ✉️ 全新：SMTP 邮件服务

用于发送注册欢迎邮件等系统通知，可在系统设置中自由开关。

- **系统设置 → 邮件**：新增独立标签页
  - 一键开关邮件服务（关闭时注册不发送邮件）
  - SMTP 服务器 / 端口 / 加密方式（SSL 465 或 STARTTLS 587）
  - SMTP 账号 / 密码（授权码）/ 发件邮箱 / 发件人名称
  - **发送测试邮件**：填写邮箱即可即时验证配置是否正确
- **注册流程**：新增「邮箱（可选）」字段
  - 邮箱自动存入用户资料
  - SMTP 开启且填写了邮箱时，注册成功即自动发送**欢迎邮件**（含账号、邮箱、登录地址）
  - 采用"尽力而为"：邮件发送失败不会阻断注册
- 自动兼容自签 / 内网 SMTP 证书；支持 QQ / 163 等主流邮箱（使用授权码）

## 🎨 系统设置全新布局

彻底重做系统设置页面，从混乱的双栏布局改为清晰的**6 标签页**结构：

- **通用**：应用名称 / Logo 上传 / 版权 / 联系邮箱 / 公告 / 关于
- **安全**：注册开关 / 密码最小长度 / 会话超时 / 登录验证码阈值
- **存储**：分片大小 / 单文件上限 / 分享默认有效期 / 回收站保留天数
- **外观**：品牌色预设 / 主题风格 / 自定义背景
- **导航**：菜单拖拽排序
- **更新**：在线检查更新

修复了旧版中多个区块重复出现的问题，保存栏固定在底部，整体更清爽易用。

## 技术细节

- 新增 `nodemailer` 依赖（v9.0.5）
- 新增 `email.service.ts`：SMTP 配置读取、欢迎邮件、测试邮件
- 新增 `POST /settings/smtp/test` 测试邮件接口
- SMTP 配置以键值形式存入 settings 表（smtp* 键）
